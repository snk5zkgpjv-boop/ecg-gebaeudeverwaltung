import {neon} from '@neondatabase/serverless';import {sessionUser,appState,profileFor,publicUser} from '../../lib/auth.js';
export default async function handler(req,res){if(req.method!=='GET')return res.status(405).json({error:'Methode nicht erlaubt'});if(!process.env.DATABASE_URL)return res.status(500).json({error:'DATABASE_URL fehlt'});const sql=neon(process.env.DATABASE_URL),user=await sessionUser(req,sql);if(!user)return res.status(401).json({error:'Nicht angemeldet'});const state=await appState(sql),profile=profileFor(state,user);if(!profile)return res.status(403).json({error:'Nicht freigeschaltet'});return res.status(200).json({user:publicUser(user,profile)})}
